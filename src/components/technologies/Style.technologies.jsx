import styled from "styled-components";

export const StyleTechnologiesSection = styled.section`
  div {
    margin: 3rem 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    place-items: center;

    img {
      width: 60px;
    }
  }
`;
